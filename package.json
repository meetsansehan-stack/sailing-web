{
  "name": "parenting-newsletter",
  "version": "0.1.0",
  "private": true,
  "description": "Daily parenting newsletter service",
  "scripts": {
    "pipeline:daily": "pnpm --filter @parenting-newsletter/api run pipeline:daily",
    "pipeline:publish": "pnpm --filter @parenting-newsletter/api run pipeline:publish",
    "pipeline:run": "pnpm --filter @parenting-newsletter/api run pipeline:daily",
    "agent:test": "node scripts/agent-test.js",
    "pipeline:dry-run": "node scripts/pipeline-dry-run.js"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.17.1",
    "typescript": "^5.3.3",
    "zod": "^3.22.4"
  }
}
