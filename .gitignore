# Dependencies
node_modules
# pnpm-lock.yaml은 커밋 — 배포(Vercel/Railway)의 --frozen-lockfile 재현 설치에 필요

# Build
dist
.next
.turbo
*.tsbuildinfo

# 수요 발굴 — 생성 데이터(JSON). 리포트(.md)·대시보드(.html)는 커밋, 원자료는 재생성
packages/db/scripts/demand/out/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode
.idea
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Prisma migrations는 커밋 — 스키마 이력·배포 재현. (CLAUDE.md: 직접 "수정" 금지일 뿐 버전관리는 함)
packages/db/backups/
.vercel
.env*
